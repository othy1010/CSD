jest.mock('@ng-bootstrap/ng-bootstrap');

import { ComponentFixture, TestBed, inject, fakeAsync, tick } from '@angular/core/testing';
import { HttpResponse } from '@angular/common/http';
import { HttpClientTestingModule } from '@angular/common/http/testing';
import { of } from 'rxjs';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

import { DecisionPatternService } from '../service/decision-pattern.service';

import { DecisionPatternDeleteDialogComponent } from './decision-pattern-delete-dialog.component';

describe('DecisionPattern Management Delete Component', () => {
  let comp: DecisionPatternDeleteDialogComponent;
  let fixture: ComponentFixture<DecisionPatternDeleteDialogComponent>;
  let service: DecisionPatternService;
  let mockActiveModal: NgbActiveModal;

  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [HttpClientTestingModule],
      declarations: [DecisionPatternDeleteDialogComponent],
      providers: [NgbActiveModal],
    })
      .overrideTemplate(DecisionPatternDeleteDialogComponent, '')
      .compileComponents();
    fixture = TestBed.createComponent(DecisionPatternDeleteDialogComponent);
    comp = fixture.componentInstance;
    service = TestBed.inject(DecisionPatternService);
    mockActiveModal = TestBed.inject(NgbActiveModal);
  });

  describe('confirmDelete', () => {
    it('Should call delete service on confirmDelete', inject(
      [],
      fakeAsync(() => {
        // GIVEN
        jest.spyOn(service, 'delete').mockReturnValue(of(new HttpResponse({ body: {} })));

        // WHEN
        comp.confirmDelete(123);
        tick();

        // THEN
        expect(service.delete).toHaveBeenCalledWith(123);
        expect(mockActiveModal.close).toHaveBeenCalledWith('deleted');
      })
    ));

    it('Should not call delete service on clear', () => {
      // GIVEN
      jest.spyOn(service, 'delete');

      // WHEN
      comp.cancel();

      // THEN
      expect(service.delete).not.toHaveBeenCalled();
      expect(mockActiveModal.close).not.toHaveBeenCalled();
      expect(mockActiveModal.dismiss).toHaveBeenCalled();
    });
  });
});
